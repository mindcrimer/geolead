<?xml version="1.0" encoding="utf-8"?>
<jobExecution scriptVersion="0.1.1" dateCreate="{{ now|date('d.m.Y H:i:s') }}">
    {%- for race in races %}
	<job idJob="{{ job.pk }}" idUnit="{{ job.unit_id }}">

    </job>
    {%- endfor %}
</jobExecution>
